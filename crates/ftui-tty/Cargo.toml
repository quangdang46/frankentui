[package]
name = "ftui-tty"
version = "0.5.0"
edition = "2024"
license = "LicenseRef-MIT-OpenAI-Anthropic-Rider"
description = "Native Unix terminal backend for FrankenTUI (replaces Crossterm)."
repository = "https://github.com/Dicklesworthstone/frankentui"
homepage = "https://github.com/Dicklesworthstone/frankentui"
documentation = "https://docs.rs/ftui-tty"
readme = "../../README.md"
keywords = ["tui", "terminal", "ui", "ftui", "frankentui"]
categories = ["command-line-interface", "gui"]

[dependencies]
ftui-backend = { path = "../ftui-backend", version = "0.5.0" }
ftui-core = { path = "../ftui-core", version = "0.5.0" }
ftui-render = { path = "../ftui-render", version = "0.5.0" }

# Safe Unix terminal control (termios, ioctl).
[target.'cfg(unix)'.dependencies]
nix = { version = "0.31.1", features = ["term", "poll"] }
rustix = { version = "1.1.3", features = ["termios", "fs"] }
signal-hook = { version = "0.4.3", features = ["iterator"] }

[dev-dependencies]
portable-pty = "0.9.0"
proptest = "1.6.0"
